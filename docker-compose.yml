services:
  traefik:
    image: traefik:v3.3
    command:
      - "--providers.docker=true"
      - "--providers.docker.exposedbydefault=false"
      - "--entrypoints.web.address=:80"
    ports:
      - "80:80"
    volumes:
      - ${DOCKER_SOCKET_PATH:-/var/run/docker.sock}:/var/run/docker.sock:ro
    networks:
      - traefik

  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: ${DB_USERNAME:-launchpad}
      POSTGRES_USER: ${DB_USERNAME:-launchpad}
      POSTGRES_PASSWORD: ${DB_PASSWORD:-launchpad}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - traefik
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USERNAME:-launchpad}"]
      interval: 5s
      timeout: 5s
      retries: 5

  launchpad:
    image: filipn123/launchpad-backend:latest
    env_file:
      - .env
    environment:
      DB_URL: jdbc:postgresql://postgres:5432/launchpad
      DOCKER_SOCKET: unix:///var/run/docker.sock
    volumes:
      - ${DOCKER_SOCKET_PATH:-/var/run/docker.sock}:/var/run/docker.sock
    depends_on:
      postgres:
        condition: service_healthy
      traefik:
        condition: service_started
    networks:
      - traefik
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.launchpad.rule=Host(`launchpad.localhost`)"
      - "traefik.http.routers.launchpad.entrypoints=web"
      - "traefik.http.routers.launchpad-webhook.rule=PathPrefix(`/webhook`)"
      - "traefik.http.routers.launchpad-webhook.entrypoints=web"
      - "traefik.http.services.launchpad.loadbalancer.server.port=8082"

  watchtower:
    image: containrrr/watchtower
    volumes:
      - ${DOCKER_SOCKET_PATH:-/var/run/docker.sock}:/var/run/docker.sock
    environment:
      WATCHTOWER_POLL_INTERVAL: 300
    command: launchpad-launchpad-1

networks:
  traefik:
    name: traefik

volumes:
  postgres_data:
